name: Update Game Data

on:
  schedule:
    # Run daily at 00:00 UTC
    - cron: '0 0 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Run skins processing script
        run: python process_skins.py
        
      - name: Run icons processing script
        run: python process_icons.py
        
      - name: Run lanes processing script
        run: python process_lanes.py

      - name: Run items processing script
        run: python process_items.py
        
      - name: Check for changes
        id: check_changes
        run: |
          if git diff --quiet rarity.json icons.json lanes.json items.json chromas.json; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi
          
      - name: Commit and push if changed
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add rarity.json icons.json lanes.json items.json chromas.json
          git commit -m "Update game data (rarity, icons, lanes, items) [automated]"
          git push
